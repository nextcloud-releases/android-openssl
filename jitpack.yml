jdk:
  - openjdk21

before_install:
  - chmod +x build-android.sh
  - ./build-android.sh || true
  - ls -la build/install/ || true
  - ls -la openssl-*.aar || true
  - mv openssl-*.aar openssl-${VERSION}.aar || true

install:
  - mvn install:install-file
      -Dfile=openssl-${VERSION}.aar
      -DgroupId=com.github.nextcloud-deps
      -DartifactId=nextcloud-openssl
      -Dversion=${VERSION}
      -Dpackaging=aar
      -DgeneratePom=true
